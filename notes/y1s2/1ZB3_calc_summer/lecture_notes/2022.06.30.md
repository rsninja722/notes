# alternating series

form: $\sum_{n=1}^{\infin} (-1)^n b_n$ or $\sum_{n=1}^{\infin} (-1)^(n+1) b_n$

note: $(-1)^n = cos(n\pi)$

# alternating series test (AST)

and alternating series  $\sum_{n=1}^{\infin} (-1)^n b_n$ or $\sum_{n=1}^{\infin} (-1)^(n+1) b_n$ where $b_n > 0$, converges if



- 7.8 - improper integrals
    - type 1,2, mixed type
    - type 1,2 p-integrals
    - evaluation, comparisons
    - l'hopital

- geometric sum, telescopic, proof by induction (bounded? mono? other?)

- 11 - sequences that follow a function
    - alternating sequences
    - sequences by inspection (especially trig)
    - sequences and recurrence
    - sequences and induction